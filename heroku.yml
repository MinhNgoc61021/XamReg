setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: my-example-bucket
build:
  docker:
    api: api/Dockerfile
    nginx: nginx/Dockerfile
  config:
    QUIZ_ENV: production
run:
  api: uwsgi --ini uwsgi.ini



