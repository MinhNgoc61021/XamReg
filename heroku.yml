setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: bucket
build:
  docker:
    api: api/Dockerfile
  config:
    QUIZ_ENV: production
run:
  api:
    command:
      - uwsgi --ini uwsgi.ini



