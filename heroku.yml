setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    S3_BUCKET: my-example-bucket
build:
  docker:
    api: api/Dockerfile
    nginx: client/nginx/Dockerfile
    client: client/Dockerfile
  config:
    QUIZ_ENV: development
run:



